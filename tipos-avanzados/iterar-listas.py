mascotas = ["matias", "milo", "alan", "alex"]

# la funcion enumerate devuelve una tupla de los elementos de la lista
for index, mascota in enumerate(mascotas):
    # con el numero del indice y el valor que ocupa
    # con este codigo se accede  al indice-valor de la lista
    print(index, mascota)
